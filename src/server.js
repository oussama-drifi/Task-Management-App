// MirageJS mock server — no longer used.
//
// This file was an early development stub that intercepted API calls
// and served fake "posts" data from localStorage. The app now talks
// to a real Python Flask backend whose base URL is configured in .env:
//
//   VITE_API_BASE_URL=http://127.0.0.1:5000
//
// All task and subtask endpoints are handled by the backend server.
// See README.md → Backend API for the full endpoint reference.
